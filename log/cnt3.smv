MODULE main
VAR
  x : boolean;
  y : boolean;
  z : boolean;
ASSIGN
  init (x) := 0;
  next (x) := !x;
  init (y) := 0;
  next (y) := y <-> !x;
  init (z) := 0;
  next (z) := z <-> !(x & y);
SPEC
  AG !(x & y & z)
